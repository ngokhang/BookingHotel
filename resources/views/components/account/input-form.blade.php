<div class="{{$class}}">
    <label for="{{$id}}" class="d-block">{{ $title }}</label>
    <input type="text" class="form-control w-75" name="{{$name}}" id="{{$id}}" placeholder="{{$title}}" value="{{ $value }}">
  </div>